group 'com.joeyorlando'
version '1.0.0'

task clean {
    // Nothing to clean in root project.
}

task build {
    // Nothing to build in root project.
}

task test {
    // Nothing to test in root project.
}

allprojects {
    // Define heroku build process.
    task stage(dependsOn: ['test', 'build', 'clean'])
    test.mustRunAfter build
    build.mustRunAfter clean
}